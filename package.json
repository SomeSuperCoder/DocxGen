{
  "name": "docxgen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,audio,web --prefix-colors blue,cyan,red \"npm --prefix packages/backend start\" \"npm --prefix packages/backend run start:audio-service\" \"npm --prefix packages/frontend run dev\"",
    "start": "concurrently --names backend,audio --prefix-colors blue,cyan \"npm --prefix packages/backend start\" \"npm --prefix packages/backend run start:audio-service\"",
    "start:audio": "npm --prefix packages/backend run start:audio-service",
    "test": "pnpm --filter @docxgen/backend test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "eval": "pnpm --filter @docxgen/backend eval",
    "opencode:check": "pnpm --filter @docxgen/backend opencode:check",
    "max:subscribe": "pnpm --filter @docxgen/backend max:subscribe",
    "setup:env": "node scripts/setup-env.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
